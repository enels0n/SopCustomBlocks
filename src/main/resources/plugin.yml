name: ${project.name}
main: ${project.groupId}.${project.name}
version: ${project.version}
authors: [E_NeLsOn]
depend: [SopLib, NBTAPI]
api-version: 1.16
commands:
  sopcustomblocks:
    usage: /sopcustomblocks
permissions:
  sopcustomblocks.admin:
    default: op
